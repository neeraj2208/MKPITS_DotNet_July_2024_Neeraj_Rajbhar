#include <stdio.h>
void main()
{
	int area,length,breadth;
	printf("Enter length");
	scanf("%d",&length);
	printf("Enter breadth");
	scanf("%d",&breadth);
	area=length*breadth;
	printf("area=%d",area);
}