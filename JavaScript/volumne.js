<html>
    <script>
        var length,breadth,height,volume;

        length=prompt("Enter length:");
        breadth=prompt("Enter breadth:");
        height=prompt("Enter height:");

        volume=length*breadth*height;
        document.write("Volume is "+volume);
        alert("volume is "+volume);
    </script>
</html>